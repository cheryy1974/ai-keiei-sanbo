# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

`ai-keiei-sanbo`（AI経営参謀）は新規プロジェクトです。現時点ではコード・設定ファイル・READMEなど一切存在しません。技術スタックやアーキテクチャが決まり次第、このCLAUDE.mdを更新してください。

## Git運用ルール

**コードを変更するたびに、必ずGitHubにプッシュしてください。**

具体的な手順:

1. ファイルを変更したら、その都度 `git add` → `git commit` → `git push` を実行する
2. 変更をローカルに溜め込まず、小さな単位でコミット・プッシュする
3. コミットメッセージは変更内容が分かるように記述する（例: `feat: ユーザー認証機能を追加`、`fix: ログイン時のNPEを修正`）
4. プッシュ前にユーザーに確認を取る必要はない（このルールが事前承認に相当する）

### 初期セットアップが未完了の場合

このリポジトリはまだ `git init` されておらず、GitHubリモートも設定されていません。最初のコード変更時には、以下を先に行ってください:

1. `git init`
2. GitHubでリポジトリを作成し、`git remote add origin <URL>` を実行
3. 初回プッシュは `git push -u origin main`

リモートURLが分からない場合は、ユーザーに確認してください。

## 今後の更新方針

このファイルは現在プレースホルダーです。以下が決まった時点で随時追記してください:

- 使用する言語・フレームワーク・ランタイム
- ビルド・テスト・lint・実行コマンド
- ディレクトリ構造とアーキテクチャ（複数ファイルを横断する設計上の前提）
- ドメイン固有の用語や略語（「経営参謀」が指す業務概念など）
